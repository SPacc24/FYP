{
  "version": 1,
  "description": "Registry of mission playbooks. Add playbook JSON files id + path here — no Python edits required.",
  "playbooks": [
    {
      "id": "edge_to_internal_proof",
      "path": "edge_to_internal_proof.json",
      "title": "Edge to internal impact proof",
      "default": true
    },
    {
      "id": "surface_validation_only",
      "path": "surface_validation_only.json",
      "title": "Surface validation only (safe)",
      "default": false
    }
  ]
}
